 import { Text, View } from "react-native";
 import { SafeAreaView } from "react-native-safe-area-context";
 
 
 const Home = () => {
     return (
         <SafeAreaView className="bg-slate-600"> 
             <Text className='text-lg text-blue-800'>Home</Text>
         </SafeAreaView>
     );
 };
 
 export default Home;