import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";


const Rides = () => {
    return (
        <SafeAreaView className="bg-slate-600"> 
            <Text className='text-lg text-blue-800'>Rides</Text>
        </SafeAreaView>
    );
};

export default Rides;